.DS_Store

# workspace/ — projetos descartáveis para testar o toolkit.
# Fica no disco para você acompanhar, fora do git para não poluir o histórico.
workspace/

# .maestri/ — estado local da equipe multiagente (roles, sessões).
# Pertence à máquina, não ao toolkit.
.maestri/

# .claude/skills/ — symlinks para anvil/, o payload. Reconstruíveis com
# `bash .claude/skills/anvil-sync/scripts/dev-link.sh`.
# A anvil-sync é exceção: é do repositório e não vai no degit.
.claude/skills/*
!.claude/skills/anvil-sync/

# .claude/agents/ — symlinks para os agentes do payload, pelo mesmo dev-link.
.claude/agents/*

# .agents/skills/ — espelho do Codex: symlinks para .claude/skills, um por skill
# ligada, gerados pelo mesmo dev-link.
.agents/skills/*
