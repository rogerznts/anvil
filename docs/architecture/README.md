# Architecture

Desenho do sistema, stack, integrações e o glossário do domínio.

- `context.md` — o glossário ubíquo do projeto (`/anvil-domain-modeling`)
- `context-map.md` — só em repositório multi-contexto
- `adr/` — decisões arquiteturais, uma por arquivo, numeradas

Escreve aqui: `/anvil-grill`, `/anvil-architect`, `/anvil-domain-modeling`.
