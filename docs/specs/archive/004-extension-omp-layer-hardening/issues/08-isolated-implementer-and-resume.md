# 08: Implementer isolado e retomada

**What to build:** Com a isolação de tarefas do omp ligada, o `/skill:anvil-run` despacha cada implementer num workspace isolado, e o trabalho volta ao branch da spec como commits, pelo modo branch do omp. A sobra de um implementer que caiu não chega ao branch da spec, e rodar de novo retoma. Sem isolação, tudo segue como na 003, e a parada passa a dizer como sair dela. É a linha A2 do acompanhamento.

**Blocked by:** 01

**Status:** resolved
**Review:** round=1; sha=4e004f3; scope=full; verdict=fail; p1=open
**Review:** round=2; sha=d5c375c; scope=diff:4e004f3..d5c375c; verdict=pass; p1=none
**PR:** https://github.com/rogerznts/anvil/pull/5

- [x] O modo sai da configuração do omp, lida pelo `frontier.sh` com `omp config get task.isolation.enabled` e `task.isolation.merge` na linha `isolation`: com `on` (`true` e `branch`), todo despacho sai com `isolated: true`; com `off`, o laço é o da 003; com `misconfigured` (`true` e outro merge), a `anvil-run` diz isso antes de despachar, apontando `task.isolation.merge: branch`, e a parada por árvore suja continua como rede de segurança.
- [x] O relatório abre dizendo se a execução rodou com ou sem isolação.
- [x] A linha `halt` diz como sair da parada, com `git stash` ou com um commit do operador.
- [x] O manual `anvil-omp` ganha a seção de isolação: o que ela muda na retomada, que o paralelo depende dela, que precisa do modo branch, e como ligá-la na configuração do omp.
- [x] Cenário no S2 com a isolação ligada: os tickets resolvidos chegam ao branch da spec como commits, e a árvore da spec fica limpa.
- [x] Cenário no S2 com a isolação ligada e um implementer que termina sem mudar o estado: a árvore da spec fica limpa, o ticket vai para `skipped`, e uma segunda execução o despacha de novo.
- [x] Cenário no S2 com a isolação em modo patch, se o omp permitir configurá-lo: a `anvil-run` aponta a configuração antes de despachar, a árvore suja resultante dá `halt` e o relatório aponta a configuração.
- [x] O S2 sem isolação passa como antes.
- [x] O `verify` sai limpo.

## Comments

- Fonte dos fatos do omp (instalado: 18.3.0, a spec leu 18.2.11): `omp://tools/task.md`, `omp://settings.md` e o fonte em `~/.bun/install/global/node_modules/@oh-my-pi/pi-coding-agent/src/task/` (`isolation-runner.ts`, `worktree.ts`, `structured-subagent.ts`, `index.ts`), mais o schema em `dist/types/config/settings-schema.d.ts`. Chaves: `task.isolation.enabled` (padrão `false`), `task.isolation.merge` (`patch` ou `branch`, padrão `patch`), `task.isolation.apply` (padrão `true`). O campo `isolated` só entra no schema do `task` com `task.isolation.enabled` e fora do modo plan (conferido: pedindo ao modelo o schema do item, `isolated` aparece com o `.omp/config.yml` ligando a isolação e some sem ele).
- Execução real (protótipo `/tmp/anvil-004-08/proto.sh`, haiku): no modo branch, os commits do implementer chegam ao branch da spec e a árvore fica limpa; o hook do filho roda com o cwd em `~/.omp/wt/<id>/m`, no mesmo branch. O cherry-pick gera SHAs novos quando passa um segundo entre o commit e a integração (commit `0628f8d` no workspace virou `0ef27b1` no branch). Um implementer que devolve `yield` de erro sai `cancelled`: nada chega ao branch da spec, e a sobra fica num branch `omp/task/<id>`. O modo patch aplica o trabalho sem commit (`M` no ticket, HEAD parado). Os branches `omp/task/*` ficam no repositório mesmo depois de "Merged branch".
- Achado para o operador: o `sha` que o implementer grava na linha `Review:` é o do workspace isolado, e não chega ao branch da spec, que recebe o commit com outro SHA. A rodada 2 usaria de fixed point um commit fora da história do branch (hoje alcançável pelo `omp/task/<id>` que fica; o merge-base cai antes do ticket, e o diff da rodada 2 cobre o ticket inteiro). A spec não trata disso.
- Achado para o operador: a detecção do modo pelo schema do `task` depende de o modelo raciocinar sobre a própria definição do tool. Com `--thinking off`, o haiku e o `claude-sonnet-4-6` erram nos dois sentidos (listam `isolated` e escrevem "modo: sem isolação"; ou põem um `isolated` que não existe e escrevem "com isolação"). Com `--thinking low`, o haiku acertou 5 de 5 com a isolação ligada e 2 de 2 sem ela em sondas curtas, mas no S2 inteiro ainda errou o modo no A2 (rodada 7). Alternativa determinística para decidir: `omp config get task.isolation.enabled`, que é o que liga o campo, fora do modo plan. Mudar o mecanismo é decisão de spec.
- Decisão do operador, depois da rodada 7: a detecção do modo deixa de olhar o schema do `task` e passa a ler `omp config get task.isolation.enabled` e `omp config get task.isolation.merge`. `true` e `branch` dão o modo com isolação; `enabled` diferente de `true`, o laço da 003; `true` com outro merge, o aviso antes de despachar, apontando `task.isolation.merge: branch`, com a parada por árvore suja como rede de segurança. O critério 1 e o do cenário em modo patch foram reescritos para esse mecanismo, e a spec ganhou a nota nas Implementation Decisions (seções `frontier.sh` e skill `anvil-run`), sem ADR novo.
- Onde o `omp config get` roda: no `frontier.sh`, na chave nova `isolation` (`on`, `off`, `misconfigured`, com o que foi lido), e não na skill. Três motivos. O omp lê o `.omp/config.yml` do diretório em que roda e não sobe até a raiz (conferido: num repo com `enabled: true`, `omp config get task.isolation.enabled` devolve `true` na raiz e `false` num subdiretório), e o script já faz `cd` para a raiz. O supervisor lê uma chave pronta, como lê `screen`, em vez de rodar dois comandos e interpretar a saída. E a regra fica coberta pelo fixture, com um `omp` de mentira no PATH. Sem resposta do omp, a linha sai `off`, que é o laço seguro da 003. A chave entrou no mesmo commit no script, na `anvil-run` e na tabela da spec.
- `frontier.sh`: a linha `halt` ganhou a saída, "A saída é do operador: guardá-los com git stash -u, ou fazer um commit dele, e rodar a skill de novo." (`-u` porque o `git status --porcelain` conta arquivo não rastreado). O primeiro texto ("O operador decide o que fazer com eles: guardá-los com git stash -u…") fez o haiku rodar o `git stash -u` sozinho no cenário E do S2; a `anvil-run` agora proíbe guardar, commitar ou descartar o que está fora de commit.
- Fixture `/tmp/anvil-004-01/fx.sh`: o `f-arvore-suja` virou caso G com a saída escrita à mão, em `LC_ALL=pt_BR.UTF-8` e `C`, em `/opt/local/bin/bash` 5.2.15 e `/bin/bash` 3.2.57. Com a linha `isolation`, o fixture ganhou um `omp` de mentira no PATH (`ISO_ENABLED`, `ISO_MERGE`, `ISO_FAIL`), a linha `isolation: off (task.isolation.enabled: false)` à mão em todo caso G e na tradução dos casos da 003 (linha nova da tabela), e cinco casos G nos dois locales: `f-isolacao-branch` (`on`), `f-isolacao-patch` (`misconfigured`), `f-isolacao-desligada` (`false` com `branch` dá `off`), `f-isolacao-sem-omp` (omp falhando dá `off`) e `f-isolacao-patch-suja` (`misconfigured` com árvore suja dá `halt` e `next: none`). Os cinco falharam antes da mudança no script; depois dela, `fixture: 0 falha(s)` (54 casos).
- S2 (`workspace/33-omp-run/s2.sh`): cenários novos I (007, modo branch: 01 e 02 como commits, 03 que cai com sobra e `yield` de erro), I2 (despacha o 03 de novo) e P (008, modo patch: halt e ponteiro para `task.isolation.merge: branch`), ligados por `.omp/config.yml` commitado no branch da spec. O stub isolado roda no hook do `yield` do filho. Rodadas, todas com `claude-haiku-4-5` sem fallback: rodada 6 (isolação com `--thinking low`, resto `off`): 1 falha, o A abriu dizendo "com isolação" sem isolação; todos os checks de I, I2 e P passaram, e os da 003 também. Rodada 7 (tudo com `--thinking low`): 2 falhas, o A2 abriu dizendo "com isolação", e o P não apontou a configuração. Depois da rodada 7, o parágrafo do `halt` da `anvil-run` foi simplificado (com isolação, todo `halt` depois de um despacho é modo patch) e ainda não rodou no S2.
- S2 depois da decisão do operador: os checks de A e I passam a exigir a linha `isolation: off`/`on` no `frontier.sh` e, no A e no A2, que o relatório não abra com "com isolação"; o P ganhou o check de que o `frontier.sh` diz `isolation: misconfigured` e de que o supervisor aponta `task.isolation.merge` antes do primeiro despacho. Rodadas, todas com `claude-haiku-4-5` sem fallback, com o S2 inteiro (A, A2, E, B, C, C2, C3, D, T, I, I2, P; 55 checks): rodada 8 (`--thinking off`): 2 falhas, o C3 recomendou o browser QA e em seguida o archive, e o relatório do P não abriu com o modo. Depois dela, a `anvil-run` passou a dizer que a linha do modo abre todo fim, também com `halt`. Rodada 9 (`off`): 2 falhas, o I não pôs o 03 no `--skip` depois da queda, e o relatório do P de novo sem o modo. Rodada 10 (`--thinking low`): `S2: tudo passou`. Com `off`, o haiku sai do laço da 003 às vezes; o S2 volta a ter `low` como padrão, e o modo não depende mais disso.
- `vendor-sync.sh verify`: `verify: limpo` antes do commit, com a camada da rodada 10.
- Review round=1 · P1 (Spec): o `frontier.sh` só dá `halt` quando há um `next`; no último despacho em modo patch, o trabalho integrado sem commit deixa `tree: dirty`, `all_resolved: yes` e `next: none`, sem `halt` (reproduzido pelo eixo) — o supervisor recomenda `/skill:anvil-docs archive` com o trabalho fora de commit, contra a US 33 e o critério 1 (parada como rede de segurança).
- Review round=1 · P2 (Spec): sob isolação, o `sha` que o implementer grava na linha `Review:` é o do workspace isolado; com implementer real, o cherry-pick troca esse SHA — uma rodada 2 feita em outro despacho, ou a terceira do operador, mede o ticket inteiro pelo merge-base e reabre terreno aprovado, e o manual manda apagar os `omp/task/*`, onde só esse SHA existe. Pede decisão de spec.
- Review round=1 · P2 (Spec): no modo branch, um implementer que termina com `yield` normal e deixa sobra fora de commit tem a sobra commitada pelo omp com mensagem gerada (`worktree.ts` l.916-930), e ela chega ao branch da spec; o S2 só exercita o `yield` de erro — esse commit entra sem verificação nem review, e o "cai ou desiste" do manual só vale se desistir for devolver erro.
- Review round=1 · P3 (Spec): o item novo do `Nunca` da `anvil-run`, "Chamar o `task` para outra coisa que não despachar o `next`", não foi pedido — o ticket do paralelo vai ter de reescrevê-lo, porque uma leva despacha vários tickets de uma vez.
- Review round=1 · P3 (Spec): o check do P no `s2.sh` (`sed "/^T /q"` com `pipefail`) tem corrida e deu FALHA por `BrokenPipeError` no `--so-checar` — quem rodar de novo vai atrás de uma regressão que não existe.
- Review round=1 · P3 (Standards): o mapa on/off/misconfigured e o ponteiro para `task.isolation.merge: branch` se repetem na seção Isolação, em "O fim" e no parágrafo do `halt` da `anvil-run` — um modo novo ou uma chave renomeada pede três edições, e a que faltar deixa o modelo com instruções contraditórias; a repetição vem das rodadas 8 e 9 do S2.
- Review round=1 · P3 (Standards): o motivo da linha `halt`, "um implementer novo os commitaria junto", não vale com `isolation: on`, em que o omp tira a sobra do commit do agente (inferido do fonte) — o operador lê um motivo falso nesse modo, com a parada e a saída certas.
- Review round=1 · P3 (Standards): a nota da spec e o `## Comments` citam os cenários A, A2, I, P e as rodadas do S2 sem glosa, e o `s2.sh` fica fora do git — quem lê a spec não sabe o que o A2 mede.
- Review round=1 · P3 (Standards): quebras de linha irregulares na `anvil-run/SKILL.md` depois da edição (l.55 e l.78) — sem efeito de comportamento; o próximo diff sai mais ruidoso.
- Review round=1 · P3 (Standards): cada rodada do script faz até duas chamadas ao `omp`, de ~0,43 s cada — soma ~0,9 s por iteração do laço, aceitável em série.
- Correção do P1 da rodada 1: o `frontier.sh` passa a dar `halt` com a árvore suja também quando tudo está resolvido, com o motivo "o próximo passo da spec partiria deles" e a saída "que recomenda o próximo passo com a árvore limpa" (com o browser QA e o archive nomeados na linha, o haiku os recomendava para depois da parada nas rodadas 11, 14 e 17 do S2); com a árvore suja e ticket pendente sem `next` (pulado, travado ou esperando), segue sem `halt`, como na 003. A `anvil-run` não recomenda o próximo passo com `all_resolved: yes` e `halt`. Prova: caso G `f-isolacao-patch-ultimo` (modo patch, ticket resolvido fora de commit), escrito à mão, nos dois bash e dois locales; com o script do `bed9183` ele falha (falta a linha `halt`), com o novo passa, e o fixture sai `fixture: 0 falha(s)` (55 casos). No S2, cenário novo P2: o operador commita o patch do 01 e roda de novo; o 02, último, volta sem commit, e o relatório tem de parar sem recomendar browser QA nem archive. A spec não mudou: a US 33 e a decisão "a parada de sempre pega isso" já pedem a parada nesse caso.
- P3 da rodada 1 tratados junto, por serem do próprio diff: o check do P no `s2.sh` troca o `sed "/^T /q"` por um `awk` que lê a entrada inteira, sem a corrida do `BrokenPipeError`; e a quebra irregular das linhas 55 e 78 da `anvil-run/SKILL.md` foi refeita. Os outros P2 e P3 ficam registrados acima.
- Rodadas 13 a 19 do S2, com a correção do P1, todas `claude-haiku-4-5` com `--thinking low`: 2, 1, 7, 4, 1, 1 e 2 falhas. A falha que se repete desde a 14 fica no caminho da parada, nos cenários P e P2 (modo patch), e é o critério 2: o relatório abria pela parada ("**Modified:** …", "Ticket 02 resolvido") sem a linha do modo pedida pela skill, e no P também sem o ponteiro para `task.isolation.merge`. A parada, a saída pelo `git stash -u` e a ausência de browser QA e archive passaram em todas.
- Decisão do operador depois da rodada 19: o modo no caminho da parada fica determinístico. A linha `halt` do `frontier.sh` abre com o modo da execução: "execução sem isolação", "execução com isolação" ou, com `misconfigured`, "execução com isolação fora do modo branch, que integra o trabalho sem commit: falta task.isolation.merge: branch". A `anvil-run` abre o relatório com o texto da linha `halt` copiado como está, e a linha `modo:` fica para o fim sem parada. A spec registra isso na decisão **Parada** do `frontier.sh`. Não há chave nova, e a tabela da spec não muda.
- Fixture `/tmp/anvil-004-01/fx.sh`: a saída esperada de `f-arvore-suja` (`off`), `f-isolacao-patch-suja` e `f-isolacao-patch-ultimo` (`misconfigured`) foi reescrita à mão, e entrou o caso G novo `f-isolacao-branch-suja` (`on`, árvore suja). Rodada red antes do script: o `f-arvore-suja` falhou; os casos seguintes não valem como red, porque o script foi editado durante a rodada. Depois: `fixture: 0 falha(s)` (56 casos, `/opt/local/bin/bash` 5.2.15 e `/bin/bash` 3.2.57, `LC_ALL=pt_BR.UTF-8` e `C`).
- S2 rodada 20 (`claude-haiku-4-5`, `--thinking low`, sem fallback, cenários A, A2, E, B, C, C2, C3, D, T, I, I2, P e P2): `S2: tudo passou`.
- `vendor-sync.sh verify`: `verify: limpo` em `/opt/local/bin/bash` e `/bin/bash`, saídas idênticas.
- Review round=2 · o P1 da rodada 1 fechou nos dois eixos: o `frontier.sh` dá `halt` com a árvore suja e tudo resolvido, a linha abre com o modo, e o texto citado aqui bate com o que o script emite. Nenhuma regressão na árvore suja com pendência sem `next` (pulado, travado, esperando), que segue sem parada, como na 003.
- Review round=2 · P2 (Spec): a decisão **Parada** da spec registra o texto da linha, mas não o gatilho novo, e a parada com `all_resolved: yes` vale também em `isolation: off`, onde nenhuma user story a pede (a US 30 fala de parar o despacho; a US 33, do modo patch). Quem implementar o paralelo relê a spec sem esse caso e pode tirá-lo, e o operador com arquivo solto numa spec terminada deixa de receber o archive sem uma linha de spec que diga por quê.
- Review round=2 · P3 (Spec): o check do P2 no `s2.sh` usa `COM_ISO`, que casa `isolated`, e não exige `! abre "$SEM_ISO"` como o A e o I exigem. Um relatório que só repita `isolated: true` passa como "abre dizendo o modo", uma prova frouxa justamente do critério que motivou a correção.
- Review round=2 · P3 (Spec): na `anvil-run`, "ele já diz o modo, a configuração que falta e como sair" é falso com `on` e `off`, em que não falta configuração, e o modelo procura no texto algo que não existe.
- Review round=2 · P3 (Standards): o vocabulário do modo agora está em quatro lugares, os três da `anvil-run` e o `iso_mode` do `frontier.sh`, com nomes diferentes ("execução com isolação" × "modo: com isolação"). Uma chave renomeada pede quatro edições, e o operador lê dois nomes para o mesmo modo, conforme haja parada ou não. A correção piorou o P3 de repetição da rodada 1.
- Review round=2 · P3 (Standards): com `isolation: on`, a linha `halt` diz na mesma frase "execução com isolação" e "um implementer novo os commitaria junto", o que a isolação impede. A parada e a saída estão certas, mas o motivo lido é falso nesse modo. Vem do P3 da rodada 1 e ficou mais visível.
- Review round=2 · P3 (Standards): a `anvil-run/SKILL.md` ainda quebra curto nas l.50, 56 e 59, e o registro acima dá a quebra irregular como refeita. O próximo diff sai ruidoso.
- Review round=2 · P3 (Standards): `halt_why` e `halt_then` são fragmentos de frase concatenados num `echo` de 232 colunas, e um quarto caso de parada tende a quebrar a gramática da linha. Sem defeito hoje: o fixture cobre os quatro casos.
- Review round=2 · P3 (Standards): o contrato "a linha abre com o modo e o supervisor a copia como está" está escrito em três arquivos (cabeçalho do `frontier.sh`, `anvil-run/SKILL.md`, decisão **Parada** da spec), e mudar o formato pede três edições.
- Review round=2 · P3 (Standards): a decisão **Parada** da spec cita "rodadas 14 a 19 do S2 do ticket 08" sem glosa, e o `s2.sh` fica fora do git. Quem lê a spec não descobre o que o S2 mede.
