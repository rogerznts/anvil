# 05: Condutores recusam fora do perfil `docs/specs`

**What to build:** Num projeto com o tracker no GitHub, no GitLab ou em markdown local, o `/skill:anvil-run` e o `/skill:anvil-plan` recusam dizendo que os condutores só funcionam com o perfil `docs/specs` e que o fluxo segue à mão. Sem o arquivo do perfil, sugerem o `/anvil-setup`. Com o perfil `docs/specs`, nada muda. É a parte da linha A9 que entra nesta spec.

**Blocked by:** 01

**Status:** resolved
**Review:** round=1; sha=3e49a81; scope=full; verdict=pass; p1=none

- [x] O `frontier.sh` lê o título de `docs/agents/issue-tracker.md` antes de resolver a spec: outro perfil dá `refusal` com o nome do perfil achado e a frase do fluxo à mão; arquivo ausente dá `refusal` sugerindo o `/anvil-setup`.
- [x] O `stage.sh` faz a mesma leitura antes de tudo e sai com `next: refused`, o motivo em `reason` e um `do` que manda dizer o motivo e parar sem carregar nada.
- [x] A `anvil-plan` trata `next: refused` mostrando o motivo e parando, inclusive quando chamada com um pedido.
- [x] Com o perfil `docs/specs`, a saída dos dois scripts é idêntica à de antes, por fixture nos dois bash.
- [x] Fixtures com os perfis GitHub, GitLab e local markdown do `anvil-setup`, e sem o arquivo, dão a recusa esperada nos dois scripts.
- [x] O manual `anvil-omp` diz nos Limites que os condutores recusam fora do perfil `docs/specs`.
- [x] A camada continua instalada com outro perfil: a detecção do `reset-install.sh` não muda.
- [x] O `verify` sai limpo.

## Comments

- Leitura: o `frontier.sh` e o `stage.sh` ganham a mesma função `tracker_refusal`, que lê a primeira linha `# ` de `docs/agents/issue-tracker.md`. Título que contém `docs/specs` segue sem mudança. Outro título sai sem o prefixo `Issue tracker: `, como nome do perfil: `GitHub`, `GitLab`, `Local Markdown`. A leitura roda logo depois de achar a raiz do git, porque o perfil mora nela. Fora de um repositório git o comportamento é o de antes: o `frontier.sh` recusa com "fora de um repositório git" e o `stage.sh` segue com `anvil-grill`. Com a recusa, o `frontier.sh` sai com 2 e uma linha `refusal:`, e o `stage.sh` sai com `next: refused`, o mesmo texto em `reason` e um `do` que manda mostrar a linha e parar. Nenhuma chave nova: `refused` é valor novo do `next` do `stage.sh`, que a spec já descreve, e a tabela de chaves do `frontier.sh` fica como está. A sugestão sai como `/skill:anvil-setup`, a forma do omp. A spec escreve `/anvil-setup`, que é a forma do Claude Code, e a recusa só aparece no omp. A `anvil-plan` ganhou o item `next: refused`, a `anvil-run` cita o perfil entre os motivos de recusa, e os Limites do `anvil-omp` dizem que os condutores recusam, com o nome do perfil ou sugerindo o `/skill:anvil-setup`, e que a camada continua instalada.
- Fixture: `/tmp/anvil-004-01/fx.sh`. A `main` do repositório do fixture passou a ter `docs/agents/issue-tracker.md`, copiado de `anvil-docs/templates/issue-tracker-anvil.md`, e todo branch o herda. Os 38 casos anteriores seguem passando, com a mesma saída. Casos novos, com a saída escrita à mão e rodados em `LC_ALL=pt_BR.UTF-8` e `LC_ALL=C`, em `/opt/local/bin/bash` 5.2.15 e `/bin/bash` 3.2.57: `f-perfil-docs-specs` e `s-perfil-docs-specs`, a spec 019 com um ticket no frontier e o perfil do anvil, que sai com `next:` o ticket e `next: end`; `f-perfil-{github,gitlab,local}`, `f-perfil-{github,gitlab,local}-arg` (com `019 --skip 01`) e `s-perfil-{github,gitlab,local}`, os branches da mesma spec com o perfil do `anvil-setup` copiado por cima, que saem com a recusa e o nome do perfil; `f-perfil-ausente` e `s-perfil-ausente`, sem o arquivo, que sugerem o `/skill:anvil-setup`. Antes da mudança, os 11 casos de recusa falhavam nos dois bash e nos dois locales: o `frontier.sh` devolvia `next:` o ticket 01, e o `stage.sh`, `next: end`. Depois, 49 casos ok e `fixture: 0 falha(s)`. Um script descartável comparou o `frontier.sh` e o `stage.sh` de cf5c5b2, anteriores a este ticket, com os novos em todo branch do fixture com o perfil `docs/specs` (20 branches, sem argumento, com `--skip 01` e com `001`, nos dois bash e nos dois locales): 480 comparações e nenhuma diferença.
- S2 da `anvil-plan` (`workspace/34-omp-plan/s2.sh`, haiku, `claude-haiku-4-5`): cenário novo G, um branch com o perfil GitHub do `anvil-setup` e a chamada com o pedido. Confere que o `stage.sh` disse `next: refused`, que o fim da vez cita GitHub, `docs/specs` e o fluxo à mão, que só a `anvil-plan` foi lida, sem `task`, e que nada foi escrito nem commitado. Foram três rodadas. Nas três, o grill não foi carregado e nada foi escrito. Pelas conferências finais passaram duas, a 2 e a 3. A 1 não falava do fluxo à mão, e a conferência do "à mão" entrou depois dela. Na 1 e na 3 o haiku terminou oferecendo fazer o trabalho ("Entregue o script pronto", "Você quer que eu implemente o script `conta.sh` agora?"). O `do` e o item da skill passaram a proibir essa oferta (58b9a39, 36f9e73), e a rodada 3 ofereceu mesmo assim. A vez termina sem carregar nem escrever nada, e a oferta fica para a próxima vez do operador. A conferência "o fim da vez não pergunta nada" rodou na 2 e saiu, porque a 2 terminou perguntando qual caminho manual seguir, o que a própria recusa sugere. Resultado da rodada 3, sobre 36f9e73: `S2: tudo passou`.
- S2 da `anvil-run` (`workspace/33-omp-run/s2.sh`, haiku, `claude-haiku-4-5`): cenário novo T, a spec 006 com um ticket pronto e o perfil GitHub. Confere que o `frontier.sh` recusou pelo perfil GitHub, que nenhum `task` saiu, que HEAD, árvore e branch não mudaram e que o fim cita GitHub e `docs/specs`. O T passou nas três rodadas. O S2 inteiro deu 1 falha na rodada 1, no C3, em que o haiku recomendou o archive para a história dos cards. Na rodada 2 deu 3 falhas no A: o 06 foi despachado duas vezes seguidas, sem reler o frontier entre elas. Na rodada 3 deu `S2: tudo passou`. O C3 e o A rodam num projeto com o perfil `docs/specs`, em que a saída do script não mudou, como mostram o fixture e as 480 comparações.
- A camada com outro perfil: o `reset-install.sh` ficou fora do diff. Um script descartável montou um repositório com o perfil GitHub em `docs/agents/issue-tracker.md` e rodou o `reset-install.sh --from anvil`: saiu `camada omp em .omp/, nova neste update (sinal: omp no PATH, ~/.omp/)`, com 8 linhas `omp:` no lock e com `.omp/hooks/pre/guard-spec-merge.ts`, `.omp/rules/anvil-harness.md` e as três skills instaladas.
- `vendor-sync.sh verify`: `verify: limpo`, depois de cada commit.
- Review round=1 · P2 (Spec): o `do` do `refused` e o item da `anvil-plan` proíbem se oferecer para fazer o trabalho, o que a spec não pede ("um `do` que manda dizer o motivo ao operador e parar sem carregar nada"), e nenhuma conferência do S2 G mede isso; na rodada 3 o haiku ofereceu implementar o `conta.sh` mesmo assim — o contrato promete uma proibição sem prova de efeito, e o operador que responder "sim" na vez seguinte ganha código fora do fluxo à mão.
- Review round=1 · P3 (Spec): o parágrafo da `anvil-plan` sobre o pedido novo com `next` diferente de `anvil-grill` também alcança o `next: refused` — o modelo pode somar à recusa um "pedido novo começa num branch sem spec", que não é o motivo; o S2 G não mostrou isso.
- Review round=1 · P3 (Spec): o `stage.sh` lê o perfil depois de achar a raiz do git, e não "antes de tudo" — fora de um repositório git, a `anvil-plan` com pedido ainda vai para o grill; o caso é marginal e a ordem está declarada acima.
- Review round=1 · P3 (Spec): a `anvil-plan` diz que o script devolve as linhas `spec`, `next`, `reason` e `do`, e no `refused` não saem `spec:` nem `branch:` — a skill descreve uma saída que esse ramo não tem, embora o item `next: refused` só use o `reason`.
- Review round=1 · P3 (Standards): a mesma observação da linha `spec` ausente: o `refused` é o único ramo do `stage.sh` sem `spec:` e `branch:`, que saem até no caso de fora do git — quem lê a skill espera a linha `spec` sempre e não a encontra.
- Review round=1 · P3 (Standards): a `tracker_refusal` está copiada igual em `frontier.sh` e `stage.sh` — mudar o título aceito ou o texto num só faz os dois condutores recusarem de forma diferente, e só o fixture em `/tmp`, fora do git, pegaria.
