# 08: `anvil-run` conduz a spec

**What to build:** No omp, o operador chama `/skill:anvil-run 003` e vê a spec implementada ticket a ticket, sem abrir uma sessão por ticket. A sessão principal vira o supervisor: deriva o frontier do disco, despacha um `anvil-implementer` por vez, relê o disco e recalcula, até não sobrar o que despachar. Termina com um relatório e o próximo passo, sem nunca fazer QA, archive ou PR.

**Blocked by:** 07

**Status:** resolved
**Review:** round=1; sha=f436599; scope=full; verdict=fail; p1=open
**Review:** round=2; sha=fbdbc8d; scope=diff:f436599..fbdbc8d; verdict=pass; p1=none

- [x] A skill fica em `.omp/skills/`, com trava de invocação.
- [x] Sem argumento, resolve a spec pelo prefixo numérico do branch atual; fora do branch da spec, recusa e diz qual é o branch.
- [x] Frontier: `Status:` diferente de `resolved`, todos os `Blocked by` resolvidos e não travado. Travado é o ticket cuja última linha `Review:` tem `round` 2 ou mais e `verdict=fail`.
- [x] Despacha em série, sempre o menor número do frontier, e relê o disco depois de cada implementer.
- [x] Implementer que terminou sem mudar o estado do ticket não é repetido na mesma execução.
- [x] Nunca abre a terceira rodada, nunca dispara browser QA, archive ou PR.
- [x] Mostra o andamento a cada ticket.
- [x] Relatório final: com tudo resolvido, recomenda `/skill:anvil-browser-qa` quando a spec tem tela — existe `ui/` na pasta da spec ou uma user story descreve tela — e `/anvil-docs archive` quando não tem; com pendências, lista os travados com o P1 aberto e os que dependem deles.
- [x] Rodar de novo depois de uma queda retoma pelo disco.
- [x] Cenário S2: spec de fixture com três tickets e uma aresta despachada na ordem certa; ticket com `Review: round=2 … verdict=fail` pulado e o dependente listado; recusa fora do branch; recomendação com e sem `ui/`.
- [x] A skill entra no lock como `omp:`; o `verify` sai limpo.

## Comments

- A skill é `anvil/.claude/skills/anvil-update/.omp-layer/skills/anvil-run/SKILL.md`, em pt-BR como as outras peças autorais da camada, com `disable-model-invocation: true`. Ao lado dela vai o `frontier.sh`, que lê o disco e devolve o estado da spec. As duas entram no lock como `omp: skills/anvil-run/SKILL.md` e `omp: skills/anvil-run/frontier.sh`. A skill chama o script pelo caminho instalado, `.omp/skills/anvil-run/frontier.sh`, que o check 15 do `verify` confere.
- O que dá para calcular fica no script, e não na prosa: o branch e a pasta da spec, a recusa com o nome do branch da spec, o frontier, os travados, os P1 abertos deles, os dependentes transitivos, a linha `tela` e o `proximo`. O supervisor só despacha o `proximo` e para quando ele é `nenhum`. As rodadas do S2 mostraram por quê. Enquanto a decisão era da prosa, o `haiku` seguiu com a árvore suja e escreveu "P1: open" sem o achado. Com a decisão no script, as duas coisas saíram certas.
- O "estado" que decide se um implementer é repetido são os campos `status`, `reviews` e `ultima` da linha do ticket, ou seja, o `Status:` e as `Review:`. Um implementer que parou com pergunta no `## Comments`, sem mudar o `Status:`, conta como sem mudança e vai para o `--skip`. Contar o arquivo inteiro faria esse ticket ser despachado de novo a cada volta.
- Comportamento que a spec não lista: com a árvore suja e algo no frontier, o script devolve `parada` e `proximo: nenhum`, porque o implementer seguinte commitaria o que sobrou junto com o ticket dele. É o caso de uma sessão que caiu no meio de um implementer. O relatório mostra o `git status --short` e deixa a decisão com o operador.
- Divergência da US 43 — sem tela, o próximo passo recomendado é o `/anvil-docs archive` — e do critério do relatório final: a skill recomenda `/skill:anvil-docs archive`. No omp, skill só se chama por `/skill:<nome>`, e a própria spec já escreve `/skill:anvil-browser-qa`. Escolha do operador nesta sessão. A spec não foi editada.
- O implementer é bloqueante (`blocking: true`, do ticket 07), então a chamada do `task` só volta quando ele termina. A skill manda não esperar pelo `hub` e nunca despachar dois tickets ao mesmo tempo.
- Prova: o S1 (`workspace/29-codex-mirror/s1.sh`) passa com 104 checks em `/opt/local/bin/bash` e em `/bin/bash`. A 7j agora cobre a `anvil-run` junto da rule, do manual e do agente: a skill e o script entram no lock como `omp:` e em `.omp/` iguais aos do payload, a skill tem a trava, e num payload sem as peças o dry-run lista os dois arquivos como órfãos, e a execução os tira.
- Prova: o S2 (`workspace/33-omp-run/s2.sh`) passa com 26 checks, com o omp 18.2.11 e o `haiku`, em cerca de 10 minutos. Instala o payload real num fixture em `/tmp`, com uma spec de seis tickets: 01 livre, 02 bloqueado pelo 03, 03 livre, 04 travado com `round=2 … verdict=fail` e um P1 no `## Comments`, 05 bloqueado pelo 04, 06 livre. Sem argumento, o supervisor despacha 01, 03, 02 e 06, cada um uma vez, um item por chamada, e relê o `frontier.sh` antes do primeiro despacho e depois de cada um. O 06 volta sem mudar o estado e vai para o `--skip`. O relatório traz o P1 do 04 por extenso, o 05 como dependente e o 06 como pulado. Uma segunda execução só tenta o 06. Com a árvore suja, nada é despachado. As specs 002 e 003, resolvidas, recomendam `/skill:anvil-docs archive` sem `ui/` e `/skill:anvil-browser-qa` com `ui/`. Da `main`, `001` é recusado com o nome `feature/001-run`. Em nenhum cenário o supervisor troca de branch, commita, lê `anvil-docs` ou `anvil-browser-qa` nem roda merge, push ou PR. O repositório do anvil fica igual.
- P2 (prova fraca): o S2 mede o supervisor com um implementer de mentira. Um hook `pre` do fixture, no `tool_call` do `task`, roda um script que resolve o ticket ou só comenta o 06, e o agente `anvil-implementer` do fixture só tem o `yield` e devolve "stub: feito". Com o agente stub rodando o script pelo `bash`, o `haiku` saiu do roteiro duas vezes e implementou o ticket por conta própria, e com o `read` ele explorava o repositório e reclamava que faltava ferramenta. A cadeia com o implementer de verdade, e os eixos na profundidade 2 sob um supervisor, é o S2 do ticket 07 e o ponta a ponta do ticket 10. Quem ler este S2 como prova da cadeia inteira conclui mais do que ele mostra.
- P2 (prova fraca): o S2 é probabilístico. Das cinco rodadas completas, só a quinta passa nos 26 checks, e as outras motivaram mudanças. Na primeira, o fixture estava dentro de `workspace/`, o omp carregou o `CLAUDE.md` do anvil, e o `haiku` fez `cd` para a raiz do anvil. O stub mexeu nos tickets 08, 09 e 10 deste repositório, sem commit. Eles foram restaurados, e o fixture foi para `/tmp`. Na mesma rodada, o supervisor trocou de branch depois da recusa e seguiu com a árvore suja. Na segunda, o stub respondeu sozinho à pergunta do 06 e o resolveu. Na terceira, o relatório não trouxe o P1 e recomendou `/skill:anvil-docs archive`. Na quarta, o stub implementou o 01. Uma rodada que passa não mede a taxa.
- Review round=1 · P1 (Spec): a tela só é testada pela pasta `ui/`. O critério pede o browser QA quando "existe `ui/` na pasta da spec ou uma user story descreve tela", e nenhum passo da skill manda ler a `spec.md`. No cenário B, o supervisor justificou o archive com "no ui/ folder". Uma spec cuja tela está só nas user stories recebe `/skill:anvil-docs archive`, e o operador arquiva sem o QA de browser que a US 42 (com tela, o relatório recomenda o browser QA) pede.
- Review round=1 · P2 (Spec): a `parada` com a árvore suja não está na spec. Qualquer caminho fora de commit, mesmo alheio ao ticket, zera o despacho, e isso inclui a queda no meio de um implementer, que é o caso da US 45 (rodar de novo retoma de onde parou). Quem lê a spec espera retomada, recebe parada e só descobre na primeira execução.
- Review round=1 · P2 (Standards) e P3 (Spec): o `p1_aberto` pega toda linha `Review round=N` do `## Comments` que cita "P1", como "Review round=2 · P2 (Standards): prova fraca do P1 anterior", e o ticket 07 tem uma linha assim. O relatório lista um P2 entre os P1 abertos do travado, e o operador decide a terceira rodada vendo um achado a mais.
- Review round=1 · P3 (Standards e Spec): o `Blocked by` leva todo número da linha. "03, ver ADR-0011" vira bloqueio por um ticket 11 que não existe, e o ticket nunca entra no frontier. A linha do ticket no script mostra a causa.
- Review round=1 · P3 (Standards): o `frontier.sh` tem identificadores e chaves de saída em português (`recusa`, `pular`, `proximo`, `tudo_resolvido`), contra a regra de idioma do `CLAUDE.md` para código. É o padrão que o `reset-install.sh` já segue. Nada quebra, e fica registrado que a casa diverge da regra escrita.
- Review round=1 · P3 (Standards): nomes curtos no `frontier.sh` (`bl`, `st`, `rv`, `u`, `r`, `v`, `bb`, `fecho`) e repetições pequenas (`${branch:-HEAD destacado}` três vezes, o corte do prefixo numérico duas). Quem mantém o script precisa do comentário do cabeçalho para ler os arrays, e muda o rótulo em três lugares.
- Correção da rodada 1. A linha `tela` do `frontier.sh` passa a dizer `sim` ou `nao`, com o motivo: a pasta `ui/`, ou uma user story da `spec.md` com palavra de tela (tela, página, painel, formulário, interface, navegador, botão, dashboard, UI, screen, page e afins), sem distinguir maiúsculas e só a palavra inteira, para `paginar` não contar. Com tudo resolvido e sem `ui/`, as user stories saem em linhas `historia`, e a skill deixa o supervisor subir um `nao` para o browser QA quando uma história descreve tela com outras palavras, mas nunca trocar um `sim` por archive. Enquanto a regra era só prosa, o `haiku` leu "página no navegador" e recomendou archive duas vezes, com o argumento de que não havia `ui/`. Junto, fecham dois achados do mesmo arquivo: o `p1_aberto` só pega `Review round=N · P1`, e o `Blocked by` só lê a lista de números do começo da linha. Nesse segundo conserto, a primeira versão com `sed -n '1s/…//p'` deixava de imprimir uma linha sem nada a cortar, como `03`, e desbloqueava o ticket. O fixture em `/tmp` pegou isso antes do commit.
- Correção da rodada 1, na skill. O passo do despacho diz que o que o implementer devolve não muda o laço, e que o supervisor não lê o ticket nem termina o trabalho dele. Numa rodada do S2, depois de um implementer que falhou, o `haiku` leu os tickets e escreveu o `lib.sh` ele mesmo. O passo 5 manda rodar o script mais uma vez, já com o `--skip`, antes de decidir. Numa rodada, o supervisor não repetiu o 06, mas também não chegou a rodar o script com ele no `--skip`.
- Falso positivo conhecido: na própria spec 003, a linha `tela` dá `sim` pela US 42, que fala de tela sem descrever uma, e o `anvil-run` recomendaria o browser QA ao fim dela. O relatório cita a história que decidiu, e o operador vê o motivo. Errar para o lado do QA custa menos que o contrário, que era o P1.
- Prova da correção: o S1 passa com 104 checks em `/opt/local/bin/bash` e em `/bin/bash`. O S2 passa com 29 checks, em cerca de 3 minutos, e ganhou o cenário C2: a spec 004, resolvida, sem `ui/` e com a user story "quero uma página no navegador com o extrato do dia", recebe `tela: sim (a user story 1 fala de tela)` e a recomendação `/skill:anvil-browser-qa`. O B, cuja história fala de somar uma coluna, segue com o archive. O hook do fixture passou a extrair o caminho do texto do `task`, porque o supervisor escreveu "Implement <caminho>" numa rodada, e o agente stub ficou só com o `yield`. Depois da correção, foram quatro rodadas completas: a primeira falhou no `--skip` e no C2; a segunda, com o supervisor implementando o ticket e o stub recusando "Implement <caminho>"; a terceira, só no C2; a quarta passa.
- Review round=2 · os dois eixos passam sem P1, e o P1 da rodada 1 está fechado. O eixo Spec montou fixtures em `/tmp` e conferiu nos dois bash: `tela: sim` pela `ui/` e por "página", "BOTÃO", "Tela" e "UI" numa user story; `tela: nao` com "paginar"; a palavra "tela" fora das User Stories não conta. No `C2/turno.jsonl`, o supervisor recomenda `/skill:anvil-browser-qa` sem executá-lo. O eixo Standards conferiu que o `p1_aberto` só pega `· P1`, também com o ponto médio em locale `C`, e que "03, ver ADR-0011" bloqueia só pelo 03.
- Review round=2 · P2 (Spec e Standards): regressão da correção no `Blocked by`. O corte na primeira letra que não é dígito, vírgula ou espaço faz uma linha fora de `NN, NN` falhar aberta: "01 e 02", "01 (a), 02 (b)", "`05`" e "01; 05" perdem bloqueadores. No fixture, com o bloqueador travado, o dependente foi para o frontier e sumiu de `dependem_de_travado`. Antes, essas linhas ficavam bloqueadas, visíveis. O perfil fixa `Blocked by: NN, NN`, e as 25 linhas do repositório seguem esse formato, mas o template da `anvil-to-tickets` aceita "numbers/titles". Se ficar, um ticket assim é despachado em cima de um bloqueador travado. Tirar só os tokens `ADR-NNNN` resolveria o caso da rodada 1 sem abrir os outros.
- Review round=2 · P3 (Standards): com `LC_ALL=C`, o `-i` do grep BSD não iguala maiúscula acentuada, e "a PÁGINA" ou "BOTÃO" dão `tela: nao`. Em `pt_BR.UTF-8` dão `sim`. Num ambiente com locale `C`, só a leitura das linhas `historia` pelo supervisor evita o archive para uma história em caixa alta.
- Review round=2 · P3 (Spec e Standards): o `p1_aberto` não pega linha combinada com o P1 depois de outra classe, como "Review round=2 · P2 (Standards) e P1 (Spec)". O perfil pede uma linha por achado, mas este ticket grava linhas combinadas. Nesse caso o travado continua listado, e o relatório diz "nenhuma linha … P1" em vez do achado.
- Fechamento da spec · triagem dos P2/P3 abertos com o operador. Viram linha de acompanhamento em `docs/project/acompanhamento.md`: o `Blocked by` que falha aberto (A1), a parada com árvore suja contra a US 45 (A2), o `p1_aberto` com linha combinada (A3), a caixa alta acentuada sob `LC_ALL=C` (A4) e os identificadores em português do `frontier.sh` (A8). Ficam aceitos: o implementer de mentira do S2, coberto pelo ponta a ponta do ticket 10; o S2 probabilístico; os nomes curtos e as repetições do `frontier.sh`; e o falso positivo da US 42 na própria spec 003.
