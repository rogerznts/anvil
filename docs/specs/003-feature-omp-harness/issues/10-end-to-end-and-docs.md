# 10: Ponta a ponta e documentação

**What to build:** O caminho inteiro funciona num projeto de verdade, e quem chega ao anvil entende que há três harnesses e só um automatiza. Num projeto descartável: degit do payload local, `/skill:anvil-boot`, `/skill:anvil-plan` até os tickets e `/skill:anvil-run` até o relatório. O README ganha a seção do omp e do Codex, o `overview.md` cita a camada de harness, e o manifesto registra as peças autorais da camada.

**Blocked by:** 02, 04, 06, 08, 09

**Status:** resolved

- [x] Cenário ponta a ponta em `workspace/` roda do degit ao relatório da `anvil-run`, com a camada instalada pelo boot e a guarda bloqueando um merge prematuro.
- [x] O mesmo projeto aberto no Claude Code mostra a mesma lista de skills de antes da spec.
- [x] O README tem uma seção curta: o que o omp ganha, como a camada chega, os comandos, o que segue manual no Claude Code e no Codex.
- [x] O `overview.md` cita a camada de harness.
- [x] O manifesto registra `anvil-plan`, `anvil-run`, `anvil-omp` e `anvil-implementer` como autorais da camada omp.
- [x] O `verify` sai limpo.

## Comments

- O cenário é o `workspace/35-omp-e2e/`. O `e2e.sh` monta o projeto (`setup`), roda uma vez do operador numa conversa (`turn`), pede da `main` o merge do branch da spec (`guard`), commita pelo operador (`commit`) e lê a lista de skills que o Claude Code anuncia ao abrir um projeto (`claude`). O `check.sh`, com o `plan_check.py`, confere a evidência em `out/` e o histórico do projeto, sem chamar modelo. A saída da última conferência está em `check.out`.
- Duas divergências do texto da spec, "um projeto descartável em `workspace/`" e "degit do payload local". O projeto fica em `/tmp/anvil-35-omp-e2e/`, com o script e a evidência em `workspace/`. É o que os S2 dos tickets 08 e 09 fazem, porque o omp sobe pelos diretórios pais atrás de context files e o `CLAUDE.md` do anvil faria o modelo tomar a raiz dele pela do projeto. O degit recusa caminho local (`UNSUPPORTED_HOST`, conferido com `npx degit` e com `--mode=git file://`), então o degit é o `git archive <commit>:anvil`: o mesmo conteúdo que o degit baixa do tarball do ref, sem `.git`. O commit é o `HEAD` do anvil na hora do `setup`, gravado em `out/repo-head`.
- Quem responde nas conversas é o operador, uma vez por turno, depois de ler o que o modelo disse. As mensagens ficam em `out/<conversa>/<n>.msg`. O omp roda com `--model sonnet --thinking off`. O `-p` não expande `/skill:`, então o primeiro turno diz "The operator invoked /skill:<nome>. Read skill://<nome> and follow it.", como nos S2. O boot não commita e o to-spec e o to-tickets não commitaram o que publicaram, então o operador commita entre as fases: o boot, a pesquisa e o questionário na `main`, a spec e os tickets no branch da spec.
- A rodada 1 do ponta a ponta achou um defeito da `anvil-run`. Com `tela: nao`, o supervisor leu a user story 8, "receber somente o número em stdout", como algo que o usuário vê, e recomendou `/skill:anvil-browser-qa` para um comando de terminal. A US 43 — sem tela, o próximo passo é o archive — existe para que não haja QA de browser sem browser. A evidência está em `rodada1/out/U/1.fatos`, e o projeto dessa rodada em `rodada1/proj.bundle`. A correção (261f72d) restringe a subida do `nao`: a história tem de descrever uma tela que abre no navegador, e saída de terminal, stdout, stderr, código de saída, arquivo, log e API não contam. No projeto da rodada 1, com a camada atualizada pelo `reset-install.sh`, uma sessão nova da `anvil-run` recomendou `/skill:anvil-docs archive` (`rodada1/out/U2/`). A rodada 2 refez o caminho inteiro, do degit, com o payload já corrigido.
- Na mesma correção, a `anvil-run` passa a citar `.omp/skills/anvil-run/frontier.sh` num span fora do bloco `bash`, como a `anvil-plan` faz com o `stage.sh`. Com o script renomeado, o `verify` sai com `FALHA .omp-layer/skills/anvil-run/SKILL.md -> .omp/skills/anvil-run/frontier.sh`, e com o nome de volta sai limpo. A frase do ticket 08, de que o check 15 confere o caminho do `frontier.sh`, passa a ser verdadeira a partir deste commit.
- Prova: a rodada 2 passa nos 40 checks do `check.sh`, com o omp 18.2.11 e o `sonnet`, sobre o payload de ac7b1f1. Em ordem:
  - degit: o projeto recebe as skills do payload e nenhum `.omp/` nem `.agents/`, e o lock do payload não tem linha `omp:`;
  - boot, em dois turnos: roda `reset-install.sh --layers --dry-run --to .` e depois `--layers --to .`. O commit do boot tem os oito arquivos da camada em `.omp/`, iguais aos do payload, uma linha `omp:` por arquivo, um symlink relativo em `.agents/skills` por linha `skill:`, e o hook do Claude no `settings.json`;
  - `anvil-plan`, em 15 turnos numa sessão só, sem compactação: carrega o grill com o pedido. O operador responde que o log é RFC 5424 e que ninguém sabe onde a severidade vem na linha, e que incluir `warning` é decisão da Marta, que não está na conversa. O modelo nomeia os dois sinais e propõe o `anvil-research` sem o operador ter nomeado skill. Com o sim, a pesquisa grava em `docs/discovery/` uma nota que cita a RFC em `rfc-editor.org`, e o fim da vez propõe voltar ao `anvil-grill`. Com o sim, o grill volta e propõe o `anvil-to-questionnaire`, que pergunta a quem vai e o que se precisa de volta e grava o questionário. Depois, propõe voltar ao grill. Com o entendimento confirmado, propõe o `anvil-to-spec`; com a spec publicada, o `anvil-to-tickets`. Nenhuma das seis cargas saiu sem a proposta no turno anterior e o sim do operador. No fim, roda o `stage.sh` e indica `/clear` e `/skill:anvil-run 001`, sem rodar nada;
  - guarda, da `main`, com os tickets abertos: o `git merge --no-ff feature/001-conta-erros-syslog` é bloqueado com "0 de 2 tickets resolvidos" e o rodapé do script, e a `main` não anda;
  - `anvil-run`, numa sessão nova e sem argumento, em 20 minutos: frontier, implementer do 01, frontier, implementer do 02, frontier, cada despacho um `task` com um item. Cada implementer carrega `anvil-implement`, `anvil-tdd` e `anvil-code-review`, e os eixos do review rodam como filhos dele, aos pares e sem `task`. Os dois tickets passaram por uma rodada 1 reprovada e uma rodada 2 aprovada, com oito commits dos implementers no branch da spec, árvore limpa e a `main` parada. O supervisor não escreve arquivo nem roda archive, QA, merge ou push. O `conta.sh` do branch conta 2 num log com severidades 3, 4, 0 e 6, e sai com erro e stdout vazio num log com `<192>`. O relatório recomenda `/skill:anvil-docs archive`;
  - guarda depois da `anvil-run`: com 2 de 2 resolvidos, o mesmo merge é bloqueado porque a spec não foi arquivada;
  - Claude Code: aberto no projeto, anuncia as mesmas 113 skills e os mesmos comandos que num projeto com o payload de be2f7de, o ponto de onde o branch da spec saiu, e nenhuma delas é `anvil-plan`, `anvil-run` ou `anvil-omp`.
- O to-tickets propôs um ticket só nas duas rodadas. O operador pediu dois, com o 02 bloqueado pelo 01, na pausa da granularidade, para que a `anvil-run` despachasse em série com uma aresta.
- Na rodada 1, os turnos P1 a P3 rodaram sem o tool `hub` na lista do `--tools`, e a pesquisa em segundo plano não pôde ser esperada por ele. O modelo seguiu lendo a RFC e o histórico do agente, e a nota saiu. A partir do P4 da rodada 1, e na rodada 2 inteira, o `hub` está na lista. O `.tools` de cada turno registra a lista.
- Documentação: o README ganha a seção "Três harnesses, e só o omp conduz", entre o fluxo e as skills; o `overview.md` ganha a seção "Harnesses", com o termo *camada de harness* do glossário; o manifesto ganha, no comentário das autorais, a camada omp com as três skills e o agente. O `verify` sai limpo.
- P2 (prova fraca): o ponta a ponta é probabilístico, e só a rodada 2 passa inteira. A rodada 1 falhou no relatório da `anvil-run`, o defeito acima. Quem ler uma rodada verde como taxa de acerto confia mais na condução do que ela mostrou, e uma falha numa rodada futura pede outra antes de concluir regressão.
- P2 (prova fraca): os sinais dos desvios estão explícitos na fala do operador, que diz que ninguém sabe o formato e que a decisão é de quem não está na conversa. O cenário prova que a `anvil-plan` reconhece um sinal claro, propõe, espera o sim e volta à etapa de origem. Não prova que ela acha um sinal sutil numa conversa longa, e quem ler assim espera um desvio que o modelo pode não propor.
- P2 (prova fraca): dos seis desvios, rodaram o `anvil-research` e o `anvil-to-questionnaire`, os dois a partir do grill. `anvil-prototype`, `anvil-wayfinder`, `anvil-ui` e `anvil-architect` não rodaram, e nenhum desvio saiu do to-spec ou do to-tickets. O mecanismo de proposta e volta é o mesmo texto da skill para os seis, mas a skill carregada em cada um é outra, e um desvio que não volta à etapa de origem só aparece no uso.
