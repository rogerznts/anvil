# 09: Frontier em paralelo sob pedido

**What to build:** O operador com isolação pode pedir ao `/skill:anvil-run` que rode até N tickets do frontier ao mesmo tempo. Sem o pedido, a execução segue em série; sem isolação, o pedido é recusado e a execução segue em série. O relatório mede quantos tickets estavam prontos a cada rodada. É a linha A10 do acompanhamento, e revê em parte a alternativa descartada do ADR-0011.

**Blocked by:** 08

**Status:** resolved
**Review:** round=1; sha=d2eeede; scope=full; verdict=fail; p1=open
**Review:** round=2; sha=4b948e2; scope=diff:d2eeede..4b948e2; verdict=pass; p1=none
**Review:** round=3; sha=260ed19; scope=full; verdict=pass; p1=none

- [x] A `anvil-run` aceita um argumento de paralelo com N de 2 em diante.
- [x] Com isolação, cada leva é uma chamada do `task` com até N itens, um por ticket do frontier, na ordem do script; o script roda de novo depois da leva, e cada ticket passa pela comparação de antes e depois da 003.
- [x] Sem isolação, a primeira linha de andamento recusa o paralelo com o motivo, e a execução segue em série.
- [x] O `frontier.sh` emite `ready:` com quantos tickets estão no frontier, e o relatório final traz a linha `ready` de cada rodada.
- [x] A regra "nunca despachar dois tickets ao mesmo tempo" vale só sem isolação.
- [x] Conferido na prática, num fixture, o que o omp faz quando o cherry-pick de uma leva conflita, registrado no `## Comments`: o ticket que não mudou de estado vai para `skipped`, e árvore suja dá `halt`.
- [x] Cenário no S2: uma spec com dois tickets livres e independentes roda numa leva de dois, com isolação, e os dois chegam resolvidos ao branch da spec.
- [x] Cenário no S2: pedido de paralelo sem isolação é recusado e a execução segue em série.
- [x] O ADR-0012 registra a decisão do paralelo com a isolação do omp e o comportamento conferido no conflito, e revê em parte o ADR-0011.
- [x] O manual `anvil-omp` troca o limite "o frontier é em série" pelo paralelo sob pedido, com isolação.
- [x] O `verify` sai limpo.

## Comments

- Decisões do operador no começo do ticket, registradas na decisão **Paralelo** da spec. (1) Os caminhos da leva vêm de uma chave nova do `frontier.sh`, `frontier_files`, com os caminhos do frontier na ordem da linha `frontier`: a linha `ticket` não traz o arquivo, e só o `next` trazia caminho. A alternativa descartada era um `--parallel N` no script, com várias linhas `next`. (2) Com `isolation: misconfigured`, o pedido de paralelo também é recusado: no modo patch, a leva deixaria o trabalho de vários tickets misturado fora de commit antes do `halt`. As chaves `ready` e `frontier_files` entraram no mesmo commit no script, na `anvil-run` e na tabela da spec; a linha `ready` já estava na tabela.
- A `anvil-run` aceita `--parallel N`. Com N menor que 2 ou que não é número, recusa na primeira linha de andamento e segue em série. O item do `Nunca` que o P3 da rodada 1 do 08 apontou ("Chamar o `task` para outra coisa que não despachar o `next`") passou a incluir a leva.
- Conflito conferido na prática (`/tmp/anvil-004-09/conflict.sh`, saída em `/tmp/anvil-004-09/conflict.out`), com o omp 18.3.0 e o `claude-haiku-4-5`: uma chamada do `task` com dois itens isolados, no modo branch, e o stub de cada filho mudando a mesma linha do `lib.sh`, resolvendo o próprio ticket e commitando. O omp integrou cada filho quando ele terminou, pela ordem de término: o 02 terminou primeiro e entrou ("Merged branch: omp/task/ElaborateFowl"). O cherry-pick do 01 conflitou, e o omp o desfez: o resultado do `task` trouxe "Branch merge failed: omp/task/NewFlamingo … The unmerged branch remains for manual resolution", a árvore ficou limpa, sem `CHERRY_PICK_HEAD` nem stash, e os dois branches `omp/task/*` ficaram no repositório. O `frontier.sh` relido deu o 01 com `status=ready-for-agent`, igual ao de antes, e a comparação da 003 o põe em `skipped`. A árvore suja depois de uma leva só vem de um conflito na devolução do stash que o omp faz antes do cherry-pick (`src/task/worktree.ts`, lido no fonte). Como a `anvil-run` só despacha com a árvore limpa, esse caso não foi reproduzido; se acontecer, a árvore suja dá `halt`, como no 08.
- Fixture `/tmp/anvil-004-01/fx.sh`: as linhas `ready` e `frontier_files` escritas à mão em todo caso G com frontier, inclusive o `f-bloqueios` (seis tickets, na ordem) e o `f-bloqueios-pulados` (frontier vazio porque todos foram pulados, `ready: 0`). Nos casos da 003, a tradução da saída antiga as tira da linha `frontier` antiga. Red antes do script: `fixture: 25 falha(s)`, todas pela falta das duas linhas. Depois: `fixture: 0 falha(s)` (56 casos, `/opt/local/bin/bash` 5.2.15 e `/bin/bash` 3.2.57, `LC_ALL=pt_BR.UTF-8` e `C` nos casos com os dois locales).
- S2 (`workspace/33-omp-run/s2.sh`): o S2 conduz a `anvil-run` numa sessão headless do omp, com um implementer de mentira, e confere o disco e o relatório. Cenários novos, com `--parallel 2`: L (spec 009, isolação no modo branch, 01 e 02 livres: uma chamada do `task` com os dois itens e `isolated: true`, o `frontier.sh` antes e depois da leva, os dois resolvidos como commits, árvore limpa, `ready: 2` e o `ready` da rodada no relatório) e R (spec 010, sem isolação: o paralelo recusado antes do primeiro despacho, com o motivo, 01 e 02 em série, e `ready por rodada` 2 e 1 no relatório). O stub isolado passou a ler o ticket da primeira mensagem da sessão do filho, em vez de um arquivo único, para servir a dois filhos ao mesmo tempo. Rodada 21 (`claude-haiku-4-5`, `--thinking low`, sem fallback, cenários A, A2, E, B, C, C2, C3, D, T, I, I2, P, P2, L e R; 70 checks): `S2: tudo passou`.
- `vendor-sync.sh verify`: `verify: limpo` em `/opt/local/bin/bash` e `/bin/bash`, com saídas idênticas.
- Review round=1 · P1 (Spec): a `anvil-run` (Retomada), o manual `anvil-omp` e o ADR-0012 afirmam que, no conflito de uma leva, "o omp o desfaz, a árvore fica como estava, e o trabalho fica no branch `omp/task/<id>`". O omp 18.3.0 faz o cherry-pick um commit por vez e aborta só o que conflitou (`src/task/worktree.ts` l.999-1010): os commits do mesmo ticket anteriores ao conflito ficam no branch da spec. Reproduzido pelo eixo com git puro. Quem seguir o contrato redespacha o ticket em cima de parte do próprio trabalho já commitada, e reaplicar o `omp/task/<id>` à mão duplica esses commits.
- Review round=1 · P2 (Spec): o critério do conflito está marcado, mas o `skipped` vem da regra da 003 aplicada à saída do `frontier.sh`, sem a `anvil-run` rodando, e o `halt` depois de uma leva não foi reproduzido; além disso, a sonda rodou com o `frontier.sh` de antes da mudança (sem `ready` nem `frontier_files`). O caminho da árvore suja depois de uma leva fica sem observação própria.
- Review round=1 · P3 (Spec): o ADR-0012 lista o `cherry-pick --abort` entre as observações da sonda, mas ele foi lido no fonte; a sonda só mostra "Branch merge failed", a árvore limpa e nenhum `CHERRY_PICK_HEAD`. Quem lê toma fonte por observação, justamente no detalhe que esconde o P1.
- Review round=1 · P3 (Standards): o parágrafo "Numa leva…" da Retomada da `anvil-run` repete o anterior, e a história do conflito está também no `anvil-omp`, no ADR-0012 e na spec: mudar a política do conflito pede quatro edições.
- Review round=1 · P3 (Standards): o passo 4 da `anvil-run` diz "Sem isolação, nunca despache dois tickets ao mesmo tempo", mas a seção Paralelo também recusa a leva com `misconfigured`; quem ler só o laço pode achar a leva permitida no modo patch.
- Review round=1 · P3 (Standards): a skill recebe `NNN` e `--parallel N`, e o `frontier.sh` fica com o último posicional: `frontier.sh 004 --parallel 2` leria a spec 2 e recusaria (lido no código, não executado). Um supervisor que repasse os argumentos do operador ao script leva uma recusa enganosa.
- Review round=1 · P3 (Standards): o laço `files=` do `frontier.sh` repete a forma do `labels()`, e o nome `files` fica colado no array `file[]`; mudar o formato da lista pede duas edições.
- Review round=1 · P3 (Standards): a inserção de `ready` e `frontier_files` no parágrafo "Fora a recusa…" da `anvil-run` refluiu 12 linhas sem mudança de conteúdo (CLAUDE.md §3); o blame e a revisão desse trecho ficam poluídos.
- Review round=1 · P3 (Standards): o `docs/index.md` foi editado à mão, sem regenerar pelo `anvil-docs`; o índice segue com o `Last updated` antigo e "0/11 tickets resolvidos" para a spec 004. O atraso é anterior a este diff.
- Correção do P1 da rodada 1: a `anvil-run` (Retomada e lista dos pulados), o manual `anvil-omp`, o ADR-0012 e a nota da spec em "Integração do trabalho isolado" passam a dizer o que o omp faz: cherry-pick dos commits do implementer um por vez, `abort` só do que conflitou, e os commits do ticket anteriores ao conflito ficam no branch da spec, com o trabalho inteiro no `omp/task/<id>`. O ticket segue sem mudar de estado e é pulado, e o relatório, para o pulado com "Branch merge failed", nomeia o branch e avisa dos commits que já entraram; a execução seguinte o despacha sobre eles. O loop não muda, e a spec continua sem tratamento próprio do conflito.
- Prova, com o P2 e o P3 do ADR tratados junto: segunda sonda (`/tmp/anvil-004-09/conflict.sh`, saída em `/tmp/anvil-004-09/conflict2.out`, omp 18.3.0, haiku), agora com o `frontier.sh` deste ticket instalado e três commits por filho, como um implementer (um arquivo só dele, a linha disputada, o ticket resolvido). O 02 terminou primeiro e entrou inteiro; do 01, entrou o `stub: part 1 of 01` (`parte-01.txt` no branch), e não entraram a linha disputada nem o ticket resolvido. Árvore limpa, sem `CHERRY_PICK_HEAD`, 01 com `status=ready-for-agent` e `ready: 1`. O ADR-0012 separa agora o observado do lido no fonte.
- S2, cenário novo K (spec 011, isolação no modo branch, `--parallel 2`, 01 e 02 que commitam uma parte só deles e depois a mesma linha de `choque.txt`): uma chamada do `task` com os dois itens; o que terminou por último fica `ready-for-agent`, vai em `--skip`, e a parte dele de antes do choque fica no branch; o relatório cita o pulado, o `omp/task/<id>` do "Branch merge failed" e os commits que já entraram. Com isso, o `skipped` do critério do conflito é observado com a `anvil-run` rodando. O `halt` depois de uma leva segue sem reprodução, pelo motivo da sonda (a `anvil-run` só despacha com a árvore limpa); o caminho do `halt` com árvore suja é o dos casos G do fixture.
- Rodadas do S2 depois da correção, todas `claude-haiku-4-5`, `--thinking low`, sem fallback, 16 cenários (A, A2, E, B, C, C2, C3, D, T, I, I2, P, P2, L, R, K): rodada 22, 8 falhas, todas no A e no A2: o supervisor parou depois do primeiro despacho do A, com "Ready 2 remaining", sem voltar ao passo 2, e o A2 herdou a spec pela metade (a saída dessa rodada foi sobrescrita, e o registro vem da leitura feita antes). Rodada 23, 3 falhas: no I, o 03 foi despachado de novo depois da queda, com `--skip "-"`; no P, o relatório abriu com uma paráfrase da parada ("Halted: Isolation configuration issue") em vez da linha `halt` como está. Rodada 24: `S2: tudo passou` (75 checks). K, L e R passaram nas três. Nenhum check falhou duas vezes; os que falharam são do laço da 003 e do 08, e os mesmos modos de falha do haiku aparecem no registro do 08 (rodadas 9 e 14 a 19).
- `vendor-sync.sh verify` depois da correção: `verify: limpo` em `/opt/local/bin/bash` e `/bin/bash`, com saídas idênticas.
- Review round=2 · o P1 da rodada 1 fechou nos dois eixos: o texto novo da `anvil-run`, do `anvil-omp`, do ADR-0012 e da nota da spec confere com o `worktree.ts` e com o `conflict2.out`. O P2 da prova do conflito fechou pelo cenário K (5 de 5 checks nas rodadas 23 e 24), com o `halt` depois de uma leva declarado sem reprodução, e o P3 do ADR fechou.
- Review round=2 · P2 (Standards) e P3 (Spec): o aviso dos commits já integrados sai sem condição na lista de pulados da `anvil-run` (l.164-167), no `anvil-omp` (l.101-106) e no ADR-0012 (l.68-70), mas quando o primeiro commit do implementer é o que conflita o omp não integra nada, como na primeira sonda. Nesse caso o relatório anuncia commits que não existem e o manual sugere desfazê-los; o operador os procura, e pode tomar os do ticket que entrou pelos do pulado. O `git log` desfaz o engano no primeiro ciclo. Basta "os commits anteriores ao conflito, se houver".
- Review round=2 · P3 (Standards): a história do conflito estava em quatro lugares e agora está em seis (a nota da spec e a lista de pulados entraram, e o ADR a conta na Decisão e no Contra). A próxima mudança do omp pede seis edições coordenadas, e o P1 da rodada 1 nasceu de uma divergência assim. A nota da spec poderia só apontar para o ADR.
- Review round=2 · P2 (Spec), fora do diff e anterior a ele: nos cenários K das rodadas 23 e 24 e no I da rodada 24 do S2, o supervisor recomendou `/skill:anvil-docs archive` com `all_resolved: no`, contra "Com pendência, o relatório lista…" da `anvil-run`. O S2 não confere isso nesses cenários; o operador lê um próximo passo errado com ticket pendente.
- Review round=2 · os P3 da rodada 1 no passo 4, nos argumentos do `frontier.sh`, no laço `files=`, no refluxo e no `docs/index.md` seguem como estavam: o diff da correção não mexe neles.
- Review round=3 (escolha do operador, artefato inteiro; omp instalado 18.3.1) · o contrato do conflito da leva confere afirmação por afirmação com o fonte (`isolation-runner.ts` l.644-676: uma integração por filho ao terminar; `worktree.ts` l.999-1028: cherry-pick por revisão, `abort` só do que conflitou; stash antes e depois), com o `conflict2.out` e com o K da rodada 25 do S2. Fixture rodado de novo contra o HEAD: `fixture: 0 falha(s)`, 60 casos.
- Review round=3 · P2 (Standards) e P3 (Spec), segue da rodada 2: o aviso dos commits já integrados continua sem "se houver" na lista de pulados da `anvil-run` (l.164-167), na Retomada (l.195), no `anvil-omp` (l.103-106) e no ADR-0012 (l.69-70); quando o primeiro commit conflita, o relatório anuncia commits que não existem.
- Review round=3 · P2 (Spec), segue da rodada 2, agora com evidência direta: no K da rodada 25 o relatório fecha com "Próximo passo: `/skill:anvil-docs archive`" com `all_resolved: no` e o 02 pulado (`out-r25/K/turno.jsonl`); a seção "Com pendência" da `anvil-run` não veda o próximo passo por extenso e o S2 não o confere — o operador lê um próximo passo errado, e só a guarda o segura.
- Review round=3 · P3 (Spec): no mesmo K o relatório diz `Ready por rodada: 2, 1`, e o "1" é a releitura do passo 5 antes do `--skip`, sem despacho; o passo 3 não fixa que só a leitura de antes do despacho conta, e a medição sai inflada em um por pulado.
- Review round=3 · P3 (Spec): o `anvil-omp` l.94-96 diz que os `omp/task/*` ficam depois da integração, e é o observado (18.3.0 no `conflict2.out`, 18.3.1 no K), mas o fonte 18.3.1 tenta apagar o mesclado (`cleanupTaskBranches`, best-effort); "podem ficar" seria exato. As sessões não gravam a versão do omp: sondas e série 3 rodaram no 18.3.0, S2 rodada 25 e o rerun do e2e no 18.3.1.
- Review round=3 · os P3 da rodada 1 (passo 4 vs seção Paralelo, posicional do `frontier.sh`, laço `files=`, refluxo, `docs/index.md`) e o P3 da rodada 2 (história do conflito em seis lugares) seguem como estavam.
- Depois da rodada 3, por decisão do operador, os P2 e P3 de contrato (`2387987`): o aviso dos commits já integrados ganhou "se houver" na `anvil-run` (lista de pulados e Retomada), no `anvil-omp` e no ADR-0012, que também diz que nada entra quando o primeiro commit conflita; a seção "Com pendência" da `anvil-run` passou a dizer que o relatório não recomenda próximo passo, nem archive nem browser QA; o passo 3 fixa que só a leitura de antes de um despacho conta no `ready`; o passo 4 diz "fora de `isolation: on`", como a seção Paralelo; e o `anvil-omp` diz que os `omp/task/*` podem ficar depois da integração e que o de trabalho não integrado fica sempre. Sem rodada nova de review: são P2 e P3.
- Prova: o `s2.sh` ganhou o check `sem_proximo_passo` (o relatório não traz `/skill:anvil-docs archive` nem `/skill:anvil-browser-qa`) nos cenários com pendência, A, A2, I, I2 e K. Red sobre as sessões da rodada 25 (`--so-checar`): A e K falham, os dois com archive recomendado. Rodada 26 (`workspace/33-omp-run/out-r26/`), com a `anvil-run` de `2387987`, modelo `claude-sonnet-4-6` e `--thinking low` por decisão do operador, em vez do haiku das rodadas 1 a 25, 16 cenários em 10 min: `S2: tudo passou`, 75 checks, todos os turnos no modelo pedido. No K, o relatório saiu com `ready por rodada: 2`, sem a releitura do passo 5. O `verify` sai limpo nos dois bash, com saídas idênticas.
